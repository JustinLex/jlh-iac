all:
  children:
    proxmox_nodes:
      hosts:
        shodan:
          ansible_host: shodan.home.jlh.name
      vars:
        ansible_user: root
    containers:
      hosts:
        swablu:
        magneton:
      vars:
        ansible_user: root
#    vms:
#      hosts:
    webservers:
      hosts:
        swablu:
